<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ $subject }}</title>
</head>
<body>
    <h1>{{ $subject }}</h1>
    <p>{{ $body }}</p>
    {{-- <img src="{{ route('track.open', ['emailId' => $emailData['id']]) }}" alt="Pixel"> --}}
    {{-- <a href="{{ route('track.click', ['emailId' => $emailData['id']]) }}">Click here</a> --}}
</body>
</html>
